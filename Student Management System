git commit -m "Initial commit: Set up project structure, created student_management.cpp file"
git commit -m "Implemented Student struct and basic functions (add, display)"
git commit -m "Implemented search function to find students by name"
git commit -m "Added modifyGrades function to modify student grades"
git commit -m "Implemented save/load functionality to handle data persistence"
git commit -m "Improved input validation and error handling in functions"
git commit -m "Refined functionality and tested all features for final review"
git commit -m "Added comments and header documentation to the code"
git commit -m "Final review, code cleanup, and optimization"
git commit -m "Completed Major Assignment 2 - Final version of student management system"
